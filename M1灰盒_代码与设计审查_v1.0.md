# M1 灰盒 · 代码与设计审查 v1.0

> 审查对象：`E:\a Project\action-roguelike\index.html`（单文件 canvas 灰盒，1476 行）
> 对照基准：`GDD_动作肉鸽_v1.0.md` + `tuning/动作肉鸽_数值表_v1.0.xlsx`
> 审查日期：2026-09-15

---

## 0. 总体判断

**结构是对的，可以直接进入 PT-1 的准备工作——但必须先修掉 8 条 P0。**

| 维度 | 评价 |
|------|------|
| GDD 数值一致性 | 全部 22 项核心参数**逐条吻合**，无一处漂移。前摇/判定/后摇、削韧值、无敌帧、破防倍率、hitstop 全部对得上 |
| 机制实现完整度 | P2 死亡归因、破防反馈四件套**完整实现且超出要求** |
| PT-1 数据采集 | 埋点内置 + F1 面板 + ringLog ——**整个原型最有价值的部分**，但存在 4 处会让数据不可信的缺陷 |
| 数值平衡 | **存在一个会让核心决策空间塌缩的问题**（见 P0-3） |
| 工程 | 无依赖单文件、双击即测，对 PT 的物流极友好 |

### 做得比要求更好的三处

1. **前摇的方向锥**（`drawTelegraph`，line 1030-1032）。GDD 只要求"颜色 + 轮廓双通道"，代码额外画了攻击方向的扇形预告。这不只是超额完成——它回答了泛用户真正会问的问题：**不是"要不要闪"，而是"往哪闪"**。GDD 的 PT-0 目标就是测"方向判断正确率"，这个锥体直接就是 PT-0 的测试素材。
2. **死因提示是可执行的**（line 828/860 的 tip 字段）。不是"你被近战推进者打死了"，而是"看到橙红填充+粗轮廓时侧向闪避，别后退对冲"。P2 要的就是这个。
3. **`G.ringLog` 记录每次尝试的接近比**。PT-1 的核心指标本来就是"第 3 次 vs 第 1 次"，代码直接把序列打出来了。

---

## P0 · 阻断级（8 条，会影响 PT-1 数据有效性或核心机制）

### P0-1　攻击判定窗口塌缩成 1 帧 ★最严重

**位置**：`updatePlayer`，line 683-699

```js
if (atk.phase==="windup" && atk.t>=CFG.lightWindup){
  atk.phase="active"; resolveAttackHits();     // ← 只在进入 active 的那一帧调用一次
} else if (atk.phase==="active" && atk.t>=CFG.lightWindup+CFG.lightActive){
  atk.phase="recover";
}
```

`resolveAttackHits()` 只在 windup→active 的状态切换时调用**一次**。判定窗口 `lightActive = 0.08s`（约 4.8 帧）在实现上等于 1 帧。

**后果**：
- 在判定窗口的第 2-5 帧走进范围的敌人**不会被打到**。玩家会觉得"明明打到了却没伤害"。
- `CFG.lightActive` / `CFG.heavyActive` 这两个参数**完全失效**——它们不再是 tuning lever。而 GDD §4.2 把它们当作可调参数写进了规格。

**修复**：在 active 期间每帧调用。`atk.hit` 这个 Set 就是为了防重复命中而存在的，现在完全被浪费了。

```js
} else if (atk.phase==="active"){
  resolveAttackHits();                          // 每帧调用，Set 负责去重
  if (atk.t>=CFG.lightWindup+CFG.lightActive) atk.phase="recover";
}
```

### P0-2　闪避的"起手代价"没有实现

**位置**：`tryDodge`，line 770

```js
player.invuln = Math.max(player.invuln, CFG.dodgeStartup + CFG.dodgeIFrame);
```

这行把无敌时间设为 `0.04 + 0.18 = 0.22s`，并且**从第 0 帧就开始生效**。

GDD §4.2 明确写：「闪避起手 0.04s 才进入无敌。这是必须存在的'起手代价'，否则闪避会退化成无脑贴脸工具。」

**后果**：无敌帧 0.22s（而非 0.18s）且零起手。配合 0.60s 冷却，无敌覆盖率达 **0.22 / 0.82 = 27%**，而设计意图是 **0.18 / 0.82 = 22%**。玩家的实际生存能力比设计高 22%，PT-1 测出来的难度是假的。

**修复**：需要把"起手"和"无敌"分成两个计时器。

```js
// tryDodge 里
player.dodgeTimer = CFG.dodgeTotal;
player.dodgeCd = CFG.dodgeCooldown;
player.invuln = 0;                    // 不立刻无敌
player.iframeArmed = false;

// updatePlayer 里，dodgeTimer 递减之后
if (player.dodgeTimer > 0 && !player.iframeArmed &&
    player.dodgeTimer <= CFG.dodgeTotal - CFG.dodgeStartup){
  player.iframeArmed = true;
  player.invuln = Math.max(player.invuln, CFG.dodgeIFrame);
}
```

### P0-3　破防即击杀 → 削韧条和血条功能重叠 ★设计层最关键

**这不是 bug，是数值组合的后果。但它的影响比任何 bug 都大。**

按当前数值推算（非实测，由代码参数推导）：

| 路径 | 破防成本 | 破防时敌方血量 | 破防后需要 |
|------|----------|----------------|-----------|
| 非贴脸 · 5×轻击 | 5 次动作（1.80s） | 60 − 50 = **10** | **1 次轻击**（15 伤害） |
| 贴脸 · 4×轻击 | 4 次动作（1.44s） | 60 − 40 = **20** | **2 次轻击**（30 伤害） |
| 贴脸 · 1×重击 | 1 次动作（0.86s） | 60 − 26 = **34** | **3 次轻击** |

**破防窗口 1.35s 内，玩家只需要用掉 0.36-0.72s。剩余 47%-73% 的窗口被浪费。**

**这意味着**：
- 一次破防 = 一次击杀。玩家永远不需要争取第二次破防。
- 削韧条和血条**功能重叠**——敌人的 HP 实际上等于"韧性 × 2.5"。玩家只需要盯一个条。
- GDD §4.4 精心设计的**三种解法（5轻 / 2重 / 1重+1轻）在实战中全部等价**，因为都只需要一次破防。决策空间塌缩成"哪个更快"。

**而 fun hypothesis 恰恰建立在"反复安排攻击顺序"上。** 如果一局里玩家只做一次安排，那"从躲到安排"根本没有发生的机会。

**修复方向（按推荐度）**：

| 方案 | 改动 | 效果 | 代价 |
|------|------|------|------|
| **A（推荐）** | Grunt HP 60 → **100** | 一次破防打不死，必须争取两次破防 → 决策空间翻倍 | 单只击杀时间上升，需复核 PH-03 |
| B | 破防硬直 1.35s → **0.70s** | 破防变成"补刀窗口"而非"输出窗口" | 削弱破防的爽感，与 P3"成长在手上"冲突 |
| C | 破防倍率 1.5 → **1.3**，HP → 80 | 温和版，同时保留两次破防 | 改动面大，需重跑整张数值表 |

按方案 A 推算：5×轻击（50 伤害）破防 → 破防窗口 3 次轻击（45 伤害）→ 累计 95，HP 100 **不死**。韧性重置后需再走一轮 5 轻击。总动作数从 6 次升至 11 次左右。

**这个改动会让单房清空时间从 35s 左右升到 55-65s，向 GDD 的 60-90s 目标靠拢——顺手解决了 P0-4 的一半。**

### P0-4　治疗球供给是设计值的 3 倍 → 生命经济失效

**位置**：`handleDeaths`，line 1466 —— **每击杀一个敌人必掉一个治疗球**

```js
G.drops.push({x:e.x+rand(-10,10), y:e.y+rand(-10,10), r:5, life:5, pulse:rand(0,6)});
```
配合 line 747：`const heal=10;`

**推算**：
- 本房治疗供给 = 6 次击杀 × 1 球 × 10 HP = **60 HP**
- 每房受伤（GDD 目标 1-2 次受击）= 约 **18-22 HP**
- **每房净变化 = +38 ~ +42**，而 GDD 目标是 **−10 ~ +10**

**后果**：玩家几乎永远满血，生命值不构成资源。GDD §5.4 的"锯齿横盘"变成"单调上升"，P4 的情绪曲线（"我快没了"）在 M1 就已经不可能出现。血条变成装饰。

**修复**：掉落率改为 **40%**，或每房固定 2 球（清房后放置在房间内）。HP 100 → 补给 20，正好落在 GDD 的目标区间。

> 顺带一个**正面发现**：治疗球 `life=5` 会在 5 秒后消失，且掉在敌人被击杀的位置。这**天然制造了"主动接近"的行为动机**——想回血就得往敌人死掉的地方跑。这是代码里一个未经设计却服务 fun hypothesis 的涌现机制，建议**保留并写进 GDD**，而不是当 bug 修掉。

### P0-5　开局 10 秒的教学字幕会污染 PT-1

**位置**：`render`，line 1286-1297

```
"轻击 J 凑削韧 · 重击 K 一次重创 · 贴脸削韧更高 · 破防后伤害 ×1.5"
```

**这条字幕本身符合 P1 的"不超过一行文字"**，在正式版里应该保留。但在 **PT-1 里它是个方法学污染**：

PT-1 的辅助指标 PH-02 要测的是「**玩家是否主动等待破防**」。如果你在开局就告诉玩家"破防后伤害 ×1.5"，那测出来的行为是**服从指令**，不是**发现机制**。这个指标当场失去意义。

**修复**：PT-1 版本用 URL 参数或配置开关关掉这条字幕（以及 `#poise-hint` 里的"伤害 ×1.5"文案）。正式版恢复。

**建议加一个 `?pt=1` 开关，一次性解决 P0-5 和 P1-16。**

### P0-6　遥测阈值未按 dt 归一化 → 不同刷新率下指标不可比

**位置**：`recordTelemetry`，line 937-945

```js
const shrinking = nearest._lastDist!=null && nd < nearest._lastDist - 0.15;
...
approaching = along > 0.2;
```

这两个阈值是**逐帧位移量**，直接依赖帧率：
- 60Hz：玩家每帧位移 4.17px，阈值 0.2px → 几乎等价于"有任何朝敌位移"
- 144Hz：每帧位移 1.74px，阈值 0.2px → 敏感度完全不同
- 掉帧到 30Hz：每帧位移 8.33px → 判定变宽松

**PT-1 的结论建立在"第 3 次 vs 第 1 次"的比值上。如果测试机刷新率不同，这个比值就不可比。** 而 GDD 已经把"稳定 60fps"列为数据前提——但代码没有做这件事。

**修复**：改成速度阈值。

```js
const along = (pdx*ed.x + pdy*ed.y) / Math.max(dt, 1e-4);   // px/s
approaching = along > 30;                                     // 30px/s ≈ 0.12 身位/s
```

同时 **`shrinking` 与 `approaching` 是两套重叠定义，用 OR 连起来**（line 948）。两份噪声叠加会系统性抬高计数。建议只保留 dt 归一化后的 `approaching`。

### P0-7　死亡后的 430ms 仍在采集遥测

**位置**：`damagePlayer` line 668 `setTimeout(showDeath, 430)` + `loop` line 1321 `recordTelemetry()`

`recordTelemetry` 的守卫是 `if (G.state!=="playing") return;`，但死亡后的 430ms 内 `G.state` **仍然是 "playing"**。

这 430ms 里玩家不移动、不出手，`approachFrames` 不增长而 `frames` 持续 +26 帧 → **每次都系统性拉低接近比**。

**修复**：`if (G.state!=="playing" || !player.alive) return;`

### P0-8　playing 状态按 R 重开，数据丢失

**位置**：`restartFromAnywhere`，line 1421-1426

```js
if (G.state==="playing"){ G.attempt+=1; startRun(); return; }   // ← 没有 push ringLog
G.attempt+=1;
startRun();
```

只有 `showDeath`（line 1393）和 `showWin`（line 1409）会写 `ringLog`。**玩家中途按 R 重开的那些尝试，接近比数据直接丢掉。**

而 PT-1 的现实是：受试者很可能在局面失控时就按 R 重开，而不是等死。**丢掉的恰恰是"放弃"这一类最有信息量的样本。**

**修复**：把 push ringLog 提到 `restartFromAnywhere` 里统一处理。

---

## P1 · 明确偏差 / GDD 要求在实现中缺失（9 条）

### P1-9　近战闪避成功是完全静默的 ★

**位置**：`updateGrunt`，line 856-859

```js
if (player.invuln>0 || player.dodgeTimer>0){
  // 闪避成功
} else { ...damagePlayer... }
```

**注释是空的。什么都没发生。**

对比弹幕的闪避（line 822-827）：有 20% 概率弹"闪过了"文字。

**GDD §3.1 edge case 原文**：「玩家在闪避中被命中——无敌帧内判定为闪避成功，且**必须有一次清晰的'擦身而过'视觉**（残影穿过攻击）——这是泛用户学会闪避的唯一反馈通道。」

对泛用户来说，"我按了闪避然后没事"和"我按了闪避所以没事"**在感受上是两回事**。没有正反馈，玩家学不会闪避，PT-1 的失败信号 A 会假阳性——你会以为"前摇太短"，其实是"闪避没有反馈"。

**修复**：闪避成功时触发（在 `updateGrunt` 的 active 判定处）：
- `floatText(player.x, player.y-30, "闪身", "#5dffb0", 13)`
- `ring(player.x, player.y, player.r+22, "#5dffb0", 0.22, 2)`
- 一个短促的高频音（`tone(880, 0.05, "sine", 0.03, 1400)`）
- **不加慢镜、不加震屏**——正反馈要轻，不然每次都打断节奏

### P1-10　双前摇并发时无威胁层级区分

GDD §3.1 edge case：「两个敌人同时前摇——前摇的视觉层级必须能区分主次（威胁度高的前摇更亮/更大）。否则泛用户会陷入'看哪个'的瘫痪。」

当前 `drawTelegraph` 对所有敌人一视同仁。变体 2（4 Grunt + 2 Caster）必然出现并发前摇。

**修复**：在 `drawTelegraph` 里传入一个竞争度参数——计算"当前有几个敌人在前摇"以及"哪个距离最近/剩余时间最短"，给**威胁最高的那个**加 15% 半径和更高的 alpha。

### P1-11　贴脸削韧 ×1.55 让"1 重击一击破防"成立 → 决策空间塌缩

**位置**：line 265 `closePoiseMul: 1.55`（GDD 与 tuning 表**均未登记**此参数）

推算：重击削韧 18 × 1.55 = **27.9 > Grunt 韧性 20** → **贴脸一次重击直接破防**。

这直接抹掉了 GDD §4.4 设计的解空间：原本"1 重击 + 1 轻击 = 22"是**需要两步**才能破防的，现在贴脸状态下重击一步到位，且它的伤害（26）和动作数（1）都优于其他解——**这是一个严格占优策略。**

GDD 红线：「不加不能解释'制造了什么新玩家决策'的系统」。严格占优策略不是决策，是答案。

**修复选项**：

| 方案 | 改动 | 说明 |
|------|------|------|
| **A（推荐）** | 贴脸加成改为**只作用于轻击** | 保住"重击 ≠ 一击破防"，同时给"贴脸连打"一条独立的价值路径 |
| B | `closePoiseMul` 降到 **1.10** | 保住三解空间，但贴脸收益变得几乎不可感知 |
| C | Grunt 韧性 20 → **30** | 贴脸重击 27.9 < 30 不破防 ✓；但会连带拉长击杀时间，需与 P0-3 联合调 |

> 注：贴脸加成本身是对的——它是 GDD 里为**风险信号 D**（泛用户放风筝）准备的缓解措施。问题只在系数与其他参数的耦合。

### P1-12　扇面弹的实际张角是注释声称值的 2 倍，且近距离无间隙可走

**位置**：line 296 `fanSpread: 0.38, // rad 总扇面` + line 898

```js
const t = n===1?0:(i-(n-1)/2)*CFG.fanSpread;
```

`i - (n-1)/2` 对 n=3 取值为 −1, 0, +1，所以偏移是 **±0.38 rad**，**总张角 = 0.76 rad**，是注释写的"总扇面 0.38"的两倍。

**推算间隙**（相邻弹道间距 = 距离 × 0.38，减去弹体直径 12px）：

| 施法者距离 | 弹幕间隙 | 玩家碰撞宽度 40px 能否通过 |
|-----------|---------|---------------------------|
| 200px（`casterKeep` 默认站位） | 64px = **1.6 身位** | ✅ 可以通过（GDD 要求 ≥1.2 身位） |
| 120px | 33.6px = 0.84 身位 | ❌ **穿不过去** |
| 80px | 18.4px = 0.46 身位 | ❌ 完全封死 |

**后果**：贴脸打 Caster 时，三连弹是一道**无法走位穿过的墙**，只能靠无敌帧硬吃。这制造了一个不公平的难度尖峰。

更糟的是**归因提示会误导**——死亡提示写的是「扇面中间有可过间隙」（line 828），但在近距离这是**错的**。P2 要求提示可执行，这里给了一条错误的行动指令。

**修复**：让张角随距离自适应，保证最小间隙。

```js
// 在 windup 结束时
const d = dist(e.x,e.y,player.x,player.y);
const minGap = 2*player.r + 8;                        // 48px
const needed = Math.asin(clamp(minGap/(2*Math.max(d,1)),0,0.9)) * 2;
const spread = Math.max(0.38, needed / 2);            // 每发偏移
```
同时把注释改成"每发相对中心线的偏移量"，或者直接用总张角反算。

### P1-13　敌人受击硬直会暂停前摇计时 —— GDD 未登记的新增机制

**位置**：line 599 `e.stun = Math.max(e.stun, ...0.22:0.12)` + `updateEnemies` line 798

```js
if (e.stun>0){ e.stun-=dt; continue; }   // 跳过状态机 → e.timer 不推进 → 前摇被暂停
```

**这是代码里一个有实际玩法影响的机制，但 GDD §4.1/§4.3 完全没有登记。**

效果：玩家在敌人前摇期间打中它，前摇倒计时**暂停** 0.12-0.22s（轻击/重击）。也就是说**"打得够快就能推迟敌人的攻击"**。

- 对于"读前摇 → 闪避"的核心循环，这是一个**替代解法**：与其读招闪避，不如抢在敌人出手前把它打断。
- 这个替代解法的门槛是手速，而不是读招能力——**它奖励的恰恰不是 fun hypothesis 想奖励的那个能力。**

**两条路，必须选一条**：

| 方案 | 效果 |
|------|------|
| A（推荐）保留硬直，但**只在前摇以外的状态生效** | stun 仍提供"贪刀"的奖励，但无法推迟前摇 → 读招仍是唯一解 |
| B 保留现状，但**写进 GDD 并配一条递减规则**（连续命中时 stun 递减到 0） | 承认"压制流"是一条合法 build 路线，但要防止无限连 |

### P1-14　破防中的敌人仍会被击退

**位置**：`resolveAttackHits` line 601-603 —— 击退在 `e.broken>0` 时也会执行

```js
const push = norm(e.x-player.x, e.y-player.y);
e.x += push.x * (atk.type==="heavy"?14:8);
```

破防窗口只有 1.35s，敌人却会被每一次命中推开 8-14px（0.2-0.35 身位）。**重击击退 14px × 连续命中，可以把敌人推出玩家的攻击范围，白白浪费破防窗口。**

**修复**：破防期间 `push = 0`，或者减半。理由：破防的语义是"它被打散了"，此时它应该**站不住**，而不是**滑走**。

### P1-15　击杀慢镜与破防慢镜叠加，稀释破防的特殊性

**位置**：line 1465 `shake(6); hitstop(90); G.slowmo=0.15;` 每次击杀都触发

GDD §4.3 规定慢镜是"仅首次破防触发，避免后期审美疲劳"。代码在**每次击杀**也给 0.15s 慢镜。

**后果**：一局里破防 6 次 + 击杀 6 次 = 12 次慢镜。破防的慢镜被淹没在一堆慢镜里，**"破防很特殊"这个信号被自己稀释掉了**。而且 `G.slowmo=0.15` 是**直接赋值**（不是取 max），所以击杀的 0.15s 可能截断破防的 0.22s。

**修复**：击杀慢镜删掉，或降到 0.06s；赋值改为 `G.slowmo = Math.max(G.slowmo, 0.15)`。

### P1-16　破防提示文案有事实错误

**位置**：line 210

```
"破防窗口 · 伤害 ×1.5 · 贴脸削韧加成已激活"
```

「贴脸削韧加成已激活」**与破防无关**——它是位置判定（`d <= closeRange`），在破防前后都可能成立也可能不成立。这句话会教给玩家一个错误的因果。

**修复**：改成 `"破防窗口 · 伤害 ×1.5 · 现在是输出的时候"` 或直接删掉后半句。

### P1-17　前摇缺少分层音频提示

GDD §3.1 要求前摇有「视觉双通道 + **分层音频**」。代码只有 `SFX.windup()` 单音（line 347），且 Grunt 和 Caster 用的是同一个音。

破防音效做了三层（`breakSfx`，line 346）✓，说明有这个能力，只是前摇没做。

**修复**：Grunt 用低频锯齿（近身威胁），Caster 用中频正弦（远程威胁），并在前摇后半段叠加一个音高上升的提示音——**听觉上回答"还有多久出手"**，与视觉的方向锥互补。

---

## P2 · 未登记参数 / 工程（5 条）

### P2-18　一批有实战影响的参数未进 GDD 与 tuning 表

GDD 红线：「不写没有 rationale 的数值」。以下参数在代码里生效但文档里没有：

| 参数 | 位置 | 值 | 影响 |
|------|------|-----|------|
| `lightRange` / `heavyRange` | 240 / 249 | 1.55 / 1.85 身位 | 决定 trade 是否可能 |
| 击退量 | 602-603 | 轻击 8px / 重击 14px | 连击手感、破防窗口浪费（见 P1-14） |
| 受击硬直 | 599 | 轻击 0.12s / 重击 0.22s | 见 P1-13 |
| `gruntBreakTime` | 280 | 1.35s | 直接决定"一次破防能不能杀"（见 P0-3） |
| `closePoiseMul` | 265 | 1.55 | 见 P1-11 |
| 受伤后无敌 | 657 | 0.4s | 防止连击致死，需确认是有意的 |
| 击杀慢镜 | 1465 | 0.15s | 见 P1-15 |

**动作**：把这些补进 tuning 表新建的 `09_新增参数` sheet，并逐条写 rationale。

### P2-19　一个需要确认是不是巧合的数值

- 玩家轻击有效距离 = `lightRange 62px + 敌半径 18px` = **80px**
- Grunt 攻击有效距离 = `gruntAttackRange 60px + 玩家半径 20px` = **80px**

**两者一模一样。** 这意味着玩家和敌人可以在**同一帧、同一距离**互相命中——trades 会频繁发生。

如果这是有意的（"贴脸打就是互换血"），写进 GDD，它会强化"贴脸是高风险"的设定，与 P1-11 的贴脸奖励形成一对有张力的杠杆。
如果不是巧合，需要一方退让 0.1-0.15 身位。

### P2-20　逐帧创建的图形对象会影响 60fps 稳定性

**位置**：`drawBg` line 957 与 `render` line 1279 —— 每帧调用两次 `createRadialGradient`；`noise()` line 330-341 —— **每次命中都新建一个 AudioBuffer**（一局几百次）。

GDD 把"**稳定 60fps**"列为 PT-0/PT-1 的数据前提（因为前摇时序靠帧实现）。掉帧不是观感问题，是**数据有效性问题**。

**修复**：
- 背景（含网格、中心纹章、边框）渲染到离屏 canvas，只画一次
- `noise()` 的 AudioBuffer 预生成 3-4 个变体循环复用

### P2-21　6 只敌人在 3.3 秒内全部入场，构成难度尖峰

**位置**：line 1451 `G.spawnTimer=0.9` + line 1329 `G.spawnTimer=0.65`

首只单独出现后 0.9s，其余 5 只以 0.65s 间隔涌入 → **约 3.3 秒内场上从 1 只变成 6 只**。

对泛用户，这个入场速率剥夺了"逐个建立认知"的窗口。GDD 风险登记 R1 写的是「第一个房间只放 3 只 Grunt，且首只单独出现」——当前实现在**敌人数**上符合 PT-1 规格（6 只），但**入场节奏**违背了 R1 的意图。

**建议**：PT-1 保持 6 只不变（保证与 GDD 规格可比），但把错峰间隔拉到 **1.2s**，并把这个值登记为 `spawnStagger`。同时**记录受试者在第几只入场时开始慌乱**——这个数直接告诉你泛用户的并发上限是多少。

---

## 修改优先级清单

| 顺序 | 条目 | 工作量 | 为什么排这个位置 |
|------|------|--------|------------------|
| 1 | P0-1 判定窗口 | 3 行 | 修完才能相信任何打击手感 |
| 2 | P0-2 闪避起手 | 6 行 | 修完才能相信难度数据 |
| 3 | P0-3 破防即击杀 | 改 1 个数值 + 复核表 | **定义层面的问题，先定方案再改** |
| 4 | P0-5 PT-1 开关 | 10 行 | 不修则 PH-02 完全无效 |
| 5 | P0-6/07/08 遥测三条 | 15 行 | 不修则核心指标不可信 |
| 6 | P0-4 治疗球 | 2 行 | 不修则生命经济失效 |
| 7 | P1-9 闪避反馈 | 6 行 | 不修则失败信号 A 会假阳性 |
| 8 | P1-11 贴脸系数 | 1 行 + 方案选择 | 严格占优策略 |
| 9 | P1-13 硬直与前摇 | 需先定方案 | 影响核心循环的替代解法 |
| 10 | 其余 P1/P2 | — | 进 build 前清完 |

---

## 一句话总结

**数值抄得很准，机制实现得比要求更细，但"能不能相信测出来的数"这件事没做完。**

三个数据缺陷（P0-6/07/08）加上一个方法学缺陷（P0-5），会让 PT-1 的核心指标失真；而 P0-3 会让 PT-1 测出一个"玩家没有在安排顺序"的结果——**但真正的病根不在玩家，在破防窗口和敌人血量的配比。**

先修 P0-1 到 P0-8，再开 PT-1。修完这 8 条，这个灰盒就是一份合格的 PT-1 测试工具。
