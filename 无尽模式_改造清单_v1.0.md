# 无尽模式 · 改造清单 v1.0

> 审查对象：`E:\a Project\action-roguelike\index.html`（2106 行，单文件 canvas）
> 目标：把"一关固定 6 个敌人"改成"经验升级 + 海克斯 + 无上限层数"
> 配套：`海克斯模式_完整设计_v1.1.md`（内容表） · `M1灰盒_代码与设计审查_v1.0.md`（已修项）

---

## 0. 现状盘点

### 0.1 你已经做对的（上一轮审查的项都已落地）

| 项 | 位置 | 状态 |
|---|---|---|
| P0-3 破防即击杀 | `gruntHp: 100`（331） | ✅ 已改为强制两轮破防 |
| P0-4 治疗球过量 | `healBudget: 2` + 40% 掉率（2095） | ✅ 保留"往死者位置跑"的接近动机 |
| P0-5 PT-1 污染 | `PT_MODE`（272-277） | ✅ `?pt=1` 开关 |
| P0-8 丢样本 | `logAttempt("abandon")`（2047） | ✅ playing 中按 R 也记录 |
| P1-11 严格占优 | `closePoiseMul: 1.25` + `closePoiseHeavyMul: 1.0`（322-323） | ✅ 贴脸加成只作用于轻击 |
| R12 规则污染 | `rebuildCfg()`（424-464） | ✅ CFG 只读、每局重建、纯函数合并 |

另外**超出设计的新增**：角色选择（近战「刃」/ 远程「弦」，含 `fireRangerShot`）、暂停/放弃流程、海克斯升级演出（`onHexUpgrade`）。

### 0.2 结构缺口（就是"只有一关"的根因）

| # | 缺口 | 位置 |
|---|------|------|
| 1 | 胜利条件是硬编码的 `G.kills >= 6` | `loop` 1739-1743 |
| 2 | 敌人配置是硬编码的两个变体 | `spawnCombat` 1985-1993 |
| 3 | **没有经验、没有等级** | 全局 |
| 4 | 海克斯只在开局选 **1 次** | `startRun` 2081 → `openHexPick` |
| 5 | 进房即回满血 | `spawnCombat` 1980-1982 |
| 6 | 没有"下一房"的概念，没有计时分层 | `G.runTime` 在进房时归零（1973） |
| 7 | 没有结算出口，没有局外货币持久化 | `doAbandonConfirm` 1881 |
| 8 | 没有模式选择，标题界面直接是"角色 + 变体" | `showTitle` 1772-1803 |
| 9 | 海克斯池只有 3 个，选完即空 | `openHexPick` 1900-1904 |

---

## 1. 目标形态（无尽模式规格）

### 1.1 单层结构

```
1 层 = 1 个房间 = 1 波敌人
清空 → 命中出口节点？ → 是 → 结算选择（继续 / 拿走）
                      → 否 → 直接进下一层
死亡 → 按当前深度的 55% 结算
```

### 1.2 三速分离的难度增长（这是整套数值的核心）

**敌人生命、韧性、伤害必须用三个不同的增长率。**这是肉鸽难度曲线最容易做错的地方：

| 维度 | 增长方式 | 第 10 层 | 第 20 层 | 第 30 层 | 为什么是这个速度 |
|------|----------|----------|----------|----------|------------------|
| 敌人**数量** | 3 → 12 线性，封顶 12 | 7 | 12 | 12 | 缓增。屏幕不能爆，12 是泛用户的并发上限 |
| 敌人**生命** | 分段表（见下） | ×2.2 | ×3.8 | ×8.0 | 全速。这是难度的主载体 |
| 敌人**韧性** | 生命的 ×0.55 次方 | ×1.6 | ×2.1 | ×3.1 | 慢速。**破防循环必须一直可用**，否则机制在深层失效 |
| 敌人**伤害** | 生命的 ×0.4 次方 | ×1.4 | ×1.8 | ×2.3 | 最慢。泛用户不能被一击秒 |
| 治疗球**回复** | 跟随伤害 | ×14 | ×18 | ×23 | 与伤害同速，保住"锯齿横盘" |

> **如果四者同速会怎样**：第 15 层变成"敌人打你两下就死、你要打它 40 下"。这不是难度，是折磨——玩家会认为设计坏了，而不是认为自己该练。

### 1.3 难度分段表（[PLACEHOLDER]，每 5 层一个内容注入点）

| 层 | 敌人数 | 生命倍率 | 伤害倍率 | 新内容 |
|----|--------|----------|----------|--------|
| 1-3 | 3-4 | ×1.0 | ×1.0 | Grunt（教学） |
| 4-7 | 5-6 | ×1.3 | ×1.1 | + Caster |
| 8-11 | 6-7 | ×1.8 | ×1.25 | + 精英混入普通房 |
| 12-15 | 7-8 | ×2.6 | ×1.4 | + Dasher（打破"拉开就安全"） |
| 16-20 | 8-9 | ×3.8 | ×1.6 | + 敌人携带随机词条 |
| 21-25 | 9-10 | ×5.5 | ×1.8 | + 双精英房 |
| 26-30 | 10-11 | ×8.0 | ×2.05 | + 敌人破防抗性（韧性恢复 +50%） |
| 31-35 | 11-12 | ×11.5 | ×2.3 | + 复合词条 |
| 36+ | 12 | 每 5 层 ×1.45 | 每 5 层 ×1.12 | 无新内容，纯深度 |

`[PLACEHOLDER · 目标深度 · 验证路径：PT-4 测"中位玩家在第几层因数值追不上而死"。目标 25-35 层。低于 20 层说明增长过快，高于 40 层说明太慢。]`

### 1.4 等级成长（玩家的成长载体）

| 每级 | 效果 | rationale |
|------|------|-----------|
| 最大生命 | **+4** | 30 级 = +120（总 220）。这是对抗敌人伤害增长的唯一途径，否则玩家生命上限固定而伤害指数增长 → 必然一击必杀 |
| 全伤害 | **+2%** | 30 级 = ×1.6。慢于敌人生命增长（×8.0），差额由海克斯补 |
| **每 2 级** | **一次海克斯选择** | 对齐 LoL 海斗的节奏：约每 3-4 分钟一次，一局 30 层 ≈ 15 级 ≈ 7 次选择 |

**"每 2 级一次海克斯"是节奏的关键**：频率再高会变成决策疲劳（H-A），再低会让"一直玩下去"缺少期待。

### 1.5 结算出口（"能一直玩下去"的真正机制）

**每 5 层出现一次出口。**这是整个设计的收束方式：

> 不是系统无限供给内容，是**玩家主动选择要贪多深**。这让"能一直玩下去"变成玩家的决定，而不是设计的失控。

| 出口序号 | 层 | 结算产出 | 累计时间 | 单位时间产出 |
|----------|-----|----------|----------|--------------|
| 1 | 第 5 层 | 25 | 7.5 min | 3.3 /min |
| 2 | 第 10 层 | 64 | 15 min | 4.3 /min |
| 3 | 第 15 层 | 133 | 22.5 min | 5.9 /min |
| 4 | 第 20 层 | 198 | 30 min | 6.6 /min |
| 5 | 第 25 层 | 258 | 37.5 min | 6.9 /min |
| 6 | 第 30 层 | 311 | 45 min | 6.9 /min |

- 公式：`payout(k) = 25 × k^1.35`（k = 第 k 个出口）
- **主动结算 = 100%，死亡 = 55%**

> **为什么必须超线性（k^1.35 而不是 k）**：线性会让单位时间产出持平，玩家会一直在浅层刷；超线性让深度明显更赚，而深度有死亡风险——**这才构成一个真决策**。45% 的死亡损失是决策的重量。

---

## 2. 改造清单（22 项）

### A 批 · 架构地基（6 项，不做这些后面全做不了）

---

**M-01　计时拆分为 runTime / roomTime**

- **位置**：`G`（620-666）、`spawnCombat`（1973）、`updateHud`（704）、`loop`（1723）
- **现状**：`G.runTime` 和 `G.time` 都在 `spawnCombat` 里归零。一旦复用它推进下一房，整局计时就会重置
- **改成**：
  - `G.runTime`——整局累计，**只在 `startRun` 清零**
  - `G.roomTime`——本房，`beginRoom` 清零
  - `G.time`——保留作特效时钟（`Math.sin(G.time*42)` 在用）
- **难度**：低（改 4 处赋值）
- **依赖**：无（但 M-02/M-04 依赖它）

---

**M-02　房间推进与出口判定**　← **"只有一关"的根因**

- **位置**：`loop` 1739-1743
- **现状**：
  ```js
  const total = 6;
  if (G.spawnQueue.length===0 && enemies.length===0 && G.kills>=total){
    G.state="win"; showWin();
  }
  ```
- **改成**：
  ```js
  if (G.spawnQueue.length===0 && enemies.length===0 && !G.roomClearing){
    G.roomClearing = true;
    onRoomClear();          // 新函数
  }
  ```
  ```js
  function onRoomClear(){
    G.room += 1;
    if (G.room % 5 === 0) openExit();       // 出口节点
    else beginRoom();                        // 直接下一层
  }
  ```
- **新增状态**：`G.room`、`G.depth`、`G.roomClearing`、`G.state = "exit"`
- **难度**：中
- **依赖**：M-01、M-03

---

**M-03　玩家生命跨房保留**　← **无尽模式的核心张力来源**

- **位置**：`spawnCombat` 1980-1982
- **现状**：每次进房 `player.hp = player.maxHp`（回满）
- **改成**：
  - `player.maxHp` 在 `startRun` 和**升级时**从 `G.C` 重取（等级会给 +4）
  - `beginRoom` **不重置 hp**，只重置 `x/y/facing/invuln/atk/dodgeCd`
- **理由**：不回血才有"我该不该继续"的决策。回满血的无尽模式没有风险，只有疲劳。
- **难度**：低
- **依赖**：M-05（等级改 maxHp）

---

**M-04　特效清理与遥测分房**

- **位置**：`spawnCombat` 1975-1977、`recordTelemetry` 1311
- **现状**：进房清空全部 fx 与 telemetry
- **改成**：
  - fx 清理保留（`particles/ghosts/projectiles/texts/rings`）——跨房保留会累积
  - `G.drops` 清理保留——否则跨房累积会让治疗通胀
  - telemetry 改为**双层**：`G.tel`（整局）+ `G.telByRoom[]`（每房一份快照）
- **难度**：低
- **依赖**：M-02

---

**M-05　经验与等级系统**

- **位置**：`G`（新增字段）、`handleDeaths`（2088-2089 击杀处）
- **新增**：
  ```js
  G.level = 1; G.exp = 0; G.expNext = 120; G.levelUpPending = 0;
  ```
- **经验来源**：`EXP = 威胁成本 × 10`（与生成器同步，见 M-10）
  - Grunt = 10 / Caster = 12 / Dasher = 14 / 精英 = 40
- **升级曲线**：`expNext(L) = 120 + 45 × (L-1)^1.15`
  - L1→2: 120｜L2→3: 168｜L5→6: 305｜L10→11: 570
- **每级效果**：`maxHp +4`、`dmgMulLevel += 0.02`
- **每 2 级**：`G.levelUpPending += 1`（待处理的海克斯选择）
- **难度**：中
- **依赖**：无

---

**M-06　海克斯从"开局选一次"改为"升级触发"**　← **这是你要的那个感觉**

- **位置**：`startRun`（2073-2082）、`beginCombat`（1963-1970）、`applyHexPick`（1946-1956）
- **现状**：
  - `startRun()` → 直接 `openHexPick()` → `applyHexPick()` → `beginCombat()`
  - 海克斯只在开局拿一个
- **改成**：
  ```js
  function startRun(){
    ensureAudio();
    G.hexes=[]; G.hexLevels={}; G.hexCounters={}; G.hexOffer=[];
    G.room=0; G.level=1; G.exp=0; G.expNext=120; G.levelUpPending=0;
    G.roomTime=0; G.runTime=0;
    rebuildCfg();
    beginRoom();                    // ← 不再弹海克斯
  }
  ```
  ```js
  function checkLevelUp(){          // 在 handleDeaths 里累计经验后调用
    while (G.exp >= G.expNext){
      G.exp -= G.expNext; G.level += 1;
      G.levelUpPending += 1;
      G.expNext = 120 + 45*Math.pow(G.level-1, 1.15);
      player.maxHp = G.C.playerMaxHp + (G.level-1)*4;
      rebuildCfg();
      G.C.dmgMulLevel = 0.02*(G.level-1);
      floatText(player.x, player.y-40, "Lv."+G.level, "#7dffc2", 15);
      if (G.level % 2 === 0) G.hexPending = true;
    }
  }
  ```
- **`applyHexPick` 末尾必须改**：现在无条件 `beginCombat()`（会重新 spawn 全部敌人）。改成：
  ```js
  G.hexPending = false;
  if (G.roomClearing || G.stateBeforeHexPick === "playing") resumeRoom();
  else beginRoom();
  ```
- **难度**：**高**（状态机改动，容易出"选完海克斯敌人重刷"的 bug）
- **依赖**：M-02、M-05

---

### B 批 · 生成与难度（5 项）

---

**M-07　威胁预算波次生成器**

- **位置**：`spawnCombat` 1985-1993（替换硬编码的 variant 1/2）
- **现状**：两个固定配置
- **改成**：
  ```js
  const THREAT = { grunt:1.0, caster:1.2, dasher:1.4, elite:4.0 };
  function buildWave(room){
    const count = clamp(3 + Math.floor(room*0.45), 3, 12);
    const mul   = waveMul(room);                     // 见 §1.3 分段表
    const pool  = unlockPool(room);                  // 见 M-08
    const q = [];
    let eliteLeft = room >= 8 && room % 5 === 0 ? 1 : 0;   // 精英节奏
    for (let i=0;i<count;i++){
      q.push(eliteLeft-- > 0 ? "elite" : pool[(Math.random()*pool.length)|0]);
    }
    G.spawnQueue = q; G.waveMul = mul;
  }
  ```
- **难度**：中
- **依赖**：M-08、M-09

---

**M-08　敌人类型解锁表**

- **位置**：新增 `unlockPool(room)`
- **规则**：房 1-3 `["grunt"]` / 房 4+ 加 `caster` / 房 8+ 加 `elite` / 房 12+ 加 `dasher`
- **需新增敌人**：`DASHER`（`CFG.dasher*`：生命 50、韧性 16、移速 0.9、前摇 0.45、伤害 16）+ 精英配置
- **难度**：中（Dasher 要在 `updateEnemies` 1116+ 的类型分支里加一套状态机）
- **依赖**：无

---

**M-09　敌人实例参数化（支持强度倍率）**

- **位置**：`spawnEnemy`（802-815）、`makeGrunt` / `makeCaster`
- **现状**：`hp: CFG.gruntHp` 直接取常量
- **改成**：
  ```js
  function spawnEnemy(type,x,y,mul=1){
    const e = type==="grunt" ? makeGrunt(x,y) : makeCaster(x,y);
    e.hp     = e.maxHp     = Math.round(e.maxHp * mul);
    e.maxPoise = Math.round(e.maxPoise * Math.pow(mul, 0.55));
    e.dmg    = Math.round(e.dmg * Math.pow(mul, 0.40));
    e.mul = mul;
    ...
  }
  ```
- **关键**：三个指数必须不同（§1.2）。**韧性用 `mul^0.55`**——如果韧性和生命同速，深层会变成"打不死也破不了防"
- **难度**：中
- **依赖**：M-07

---

**M-10　治疗球回复随深度缩放**

- **位置**：`handleDeaths` 2095-2098 + 拾取逻辑（`heal = 10`）
- **现状**：固定 10 HP
- **改成**：`heal = Math.round(10 * Math.pow(G.waveMul, 0.40))`（与敌人伤害同速）
- **理由**：治疗固定 + 伤害指数 → 深层必死且无法通过技术改善。同速缩放后，"每房净变化"保持锯齿横盘，**难度来自容错率下降**（单次受击占生命上限的比例在涨），而不是数值差
- **难度**：低
- **依赖**：M-09

---

**M-11　遥测指标随深度分段**

- **位置**：`recordTelemetry`（1311）+ M-04 的 `G.telByRoom`
- **改成**：每房结束 push 一份快照（接近比 / 清房时间 / 受击次数 / 破防利用率）
- **用途**：**这是无尽模式最有信息量的数据**——它能直接告诉你玩家在**第几层**开始从"安排攻击顺序"退化成"逃跑"。那个层数就是难度曲线的断点，也是调参的锚点
- **难度**：低
- **依赖**：M-04

---

### C 批 · 流程与界面（9 项）

---

**M-12　模式选择界面**

- **位置**：`showTitle`（1772-1803）
- **现状**：直接是"角色 + 变体"选择。`G.variant` 的 1/2 在无尽模式里已无意义
- **改成三层**：`模式 → 角色 → 开战`
  - **基础模式**：有限单局（10 遭遇 + Boss），19 分钟，无等级
  - **无尽模式**：XP + 等级 + 海克斯 + 无上限层数，每 5 层出口
- **`G.variant` 的处理**：**降级为调试开关**（按住 Shift 显示的开发选项），不要留在正式界面
- **难度**：中（纯 UI，但涉及状态机加一层）
- **依赖**：M-06

---

**M-13　出口结算界面**

- **位置**：新增 `openExit()` / `renderExit()` / `confirmExit()`
- **新增状态**：`G.state = "exit"`
- **内容**：当前层数 / 已获海克斯 / 现在拿走的产出 / 继续的产出预估与风险提示
- **两个选项**：`拿走结算` / `继续深入（产出 ×下次倍率）`
- **键盘**：←→ 选择，Enter 确认，**不允许 Esc 关闭**（必须做选择，但可以长按 Esc 2 秒 = 视为"拿走结算"，作为逃生阀的一致处理）
- **难度**：中
- **依赖**：M-02、M-14

---

**M-14　结算函数与局外货币持久化**

- **位置**：`doAbandonConfirm`（1881）、`showDeath`（1996）、新增 `settle(reason)`
- **现状**：结算只是文案，没有任何实际存储
- **改成**：
  ```js
  function settle(reason){                 // reason: "exit" | "death" | "abandon"
    const k = Math.floor(G.depth/5);       // 已过的出口数
    const base = k > 0 ? 25 * Math.pow(k,1.35) : Math.round(G.depth*3);
    const mult = reason === "exit" ? 1.0 : 0.55;
    const gain = Math.round(base * mult);
    const s = loadSave();
    s.currency += gain;
    saveSave(s);
    return gain;
  }
  ```
- **注意**：`abandon`（主动放弃）也应该按 55%，跟死亡一致——否则玩家会"先放弃再重开"来规避死亡损失
- **难度**：中
- **依赖**：无

---

**M-15　图鉴持久化**

- **位置**：新增 `loadSave()` / `saveSave()`，用 `localStorage: "breakgulf.save"`
- **数据结构**：
  ```js
  { currency: 0, codex: {}, eggs: {}, best: { depth: 0, level: 0 }, unlocked: [] }
  // codex: { "hex_frag": "mastered" }  // undefined | "seen" | "mastered"
  ```
- **标记时机**：`mastered` 只在**通过出口主动结算**时标记（"携带通关"的定义）。死亡不标记
- **难度**：低
- **依赖**：M-13、M-14

---

**M-16　海克斯池扩容 + 重复获取 = 升级**

- **位置**：`openHexPick`（1899-1909）
- **现状**：
  ```js
  const owned = new Set(G.hexes);
  const offer = HEX_POOL.filter(h => !owned.has(h.id) && /* exclude */);
  ```
  池子只有 3 个，选完就空 → `skipHexPick` 兜底
- **改成**：抽池 = **未拥有** + **已拥有但未满级**（后者表示"升级该海克斯"）
  ```js
  const cands = HEX_POOL.filter(h => {
    const owned = G.hexes.includes(h.id);
    if (owned && (G.hexLevels[h.id]||1) >= h.maxLevel) return false;  // 已满级
    for (const ex of (h.exclude||[])) if (G.hexes.includes(ex)) return false;
    return true;
  });
  ```
- **卡片文案分流**：未拥有 → 正常卡片；已拥有 → **"升级至 Lv2"**样式（不同边框色）
- **首发池**：从 3 个扩到 **26 个**（银 10 / 金 10 / 彩 4 / 专属 2），见设计文档 §5
- **难度**：中
- **依赖**：M-06

---

**M-17　稀有度权重 + 保底**

- **位置**：`openHexPick`
- **现状**：池子刚好三档各一，等于固定配比
- **改成**：银 60% / 金 32% / 彩 8%，且硬性保证每次至少 1 个金以上；**每第 4 次选择至少 1 个彩**
- **理由**：全银的三选一在玩家感受里等于"这次选择没有意义"
- **难度**：低
- **依赖**：M-16

---

**M-18　升级条件计数器扩展 + 性能改造**

- **位置**：`bumpHexCounter`（466-480）及调用点
- **现状**：只有 `windupHits` / `breaks` / `kills` 三个 key，且用 `for (const id of G.hexes)` 线性扫
- **改成**：
  - 新增 key：`dodges`、`closeBreaks`、`windupKills`、`noHitRooms`、`dmgTaken`、`goldSpent`
  - **性能**：改为事件驱动的反查表 `Map<condKey, hexId[]>`
    ```js
  const COND_INDEX = new Map();   // 在 rebuildCfg 里重建
  function bumpHexCounter(key, n=1){
    G.hexCounters[key] = (G.hexCounters[key]||0) + n;
    for (const id of (COND_INDEX.get(key)||[])) { /* 检查升级 */ }
  }
    ```
- **理由**：53 个海克斯 × 每次命中都 `bumpHexCounter` 的线性扫会掉帧。GDD 把"稳定 60fps"列为 PT 数据有效性的前提
- **难度**：低
- **依赖**：M-16

---

**M-19　HUD 扩展（等级 / 经验条 / 层数）**　← **不做这个，玩家感知不到升级**

- **位置**：HUD 结构（229-257）+ `updateHud`（694-708）
- **现状**：`const total = 6;` 硬编码（705）；目标文案写死"肃清 · 6 名近战"
- **改成**：
  - `obj-text` → `第 N 层`
  - `obj-sub` → `场上 X · 下一层还差 Y`
  - **新增等级条**：`Lv.N` + 经验进度条（必须常驻，否则升级是"看不见的"）
  - **新增海克斯坦进度**：`升 2 级后可选海克斯`
  - `stats` 加一格 `层数`
- **难度**：低
- **依赖**：M-05

---

**M-20　暂停菜单扩展**

- **位置**：`renderPause`（1819-1840）
- **现状**：显示角色 / 用时 / 击杀 / 破防
- **改成**：加 层数、等级、已持有海克斯列表（含等级与升级进度）、**当前深度的结算产出预估**
- **理由**：玩家要能看到"我现在拿走能得多少"——这是出口决策的前提
- **难度**：低
- **依赖**：M-14

---

### D 批 · 收尾（2 项）

---

**M-21　攀升层级**

- **位置**：`rebuildCfg`（424-464）加 `G.ascensionMods`
- **改成**：新增 `ASCENSION[]` 数组，与海克斯修走**同一条合并管线**（不新开一套）
- **6 级，核心是第 5 级**：海克斯三选一 → 二选一。它不增加数值，减少选择
- **难度**：低
- **依赖**：M-06

---

**M-22　设计文档同步**

- `海克斯模式_完整设计_v1.1.md` 里的"4 次区域交界选海克斯"要改为"每 2 级一次"，"攀升层级 0-6"保留
- `GDD_动作肉鸽_v1.0.md` 的 §3.2 单局结构（10 遭遇 + Boss）要拆成两套：基础模式（保留）+ 无尽模式（新增）
- 24h 时长表重推（见 §3.3）

---

## 3. 依赖图

```
M-01 计时分层 ──┐
               ├──→ M-02 房间推进 ──┬──→ M-06 海克斯升级触发 ──┬──→ M-12 模式选择
M-03 生命保留 ──┘                   │                          ├──→ M-16 池扩容 ──┬──→ M-17 权重保底
                                   │                          │                  └──→ M-18 计数器
M-05 经验等级 ─────────────────────┘                          └──→ M-21 攀升层级
   │
   └──→ M-19 HUD 扩展

M-08 敌人类型 ──→ M-07 生成器 ──→ M-09 参数化 ──→ M-10 治疗缩放
                                     │
                                     └──→ M-11 遥测分段

M-04 特效/遥测分离 ──→ M-02 / M-11

M-13 出口界面 ──→ M-14 结算持久化 ──→ M-15 图鉴 ──→ M-20 暂停扩展
```

**最短可用路径**（想先看到"能一直打下去"）：`M-01 → M-02 → M-03 → M-05 → M-19`
这 5 项做完，你就能连续打 10 层，有等级有经验，只是还没有海克斯选择和出口。**建议先跑通这条，再往上加。**

---

## 4. 最容易做错的三件事

### 4.1 敌人生命/韧性/伤害同速增长

最常见的错误。第 15 层会变成"敌人两下打死你、你要打它 40 下"。玩家会认为**设计坏了**，而不是认为自己该练。

**三速必须分离**：生命 1.0×，韧性 ^0.55，伤害 ^0.40。

### 4.2 用 `beginCombat()` 复用为"进下一房"

现在 `applyHexPick` → `beginCombat()` → `spawnCombat()`，而 `spawnCombat` 会：
- 重置 `runTime`（整局计时归零）
- 回满血（无尽模式的风险归零）
- 清空 telemetry（整局数据丢失）

**必须拆成两个函数**：
- `beginRoom()`——进下一房：只重置位置/状态，保留 hp / runTime / hexes
- `restartRun()`——重开整局：才清空一切

这是这次改造里**唯一容易产生连锁 bug 的地方**。

### 4.3 海克斯选择界面的"恢复"路径

升级触发海克斯时，战斗是**被打断**的（`state = "hexPick"`）。选完之后不能 `beginCombat()`（会重新 spawn 一波新敌人，玩家会以为"我选海克斯重置了关卡"）。

必须记住打断前的上下文：
```js
G.hexResume = "playing";   // 从升级触发 → 回到原房间
G.hexResume = "roomClear"; // 从清房触发 → 进下一房
```
**两种上下文的行为完全不同**，必须显式区分。

---

## 5. 建议的施工顺序

| 批次 | 内容 | 完成后你能看到什么 |
|------|------|-------------------|
| **第 1 步** | M-01 → M-03 → M-02 → M-05 → M-19 | **能一直打下去**，有等级和经验条，敌人波次循环。还没有海克斯节奏和出口 |
| **第 2 步** | M-06 → M-16 → M-17 → M-18 | 升到 2/4/6 级时弹海克斯选择，池子不枯竭，重复获取 = 升级 |
| **第 3 步** | M-08 → M-07 → M-09 → M-10 | 敌人开始变多变强，Dasher 和精英入场 |
| **第 4 步** | M-13 → M-14 → M-15 → M-20 | 每 5 层的出口选择 + 局外货币落盘 + 图鉴点亮 |
| **第 5 步** | M-04 → M-11 → M-12 → M-21 | 遥测分房（可以开始调难度曲线）、模式选择、攀升层级 |

**第 1 步做完就先玩一遍。** 不需要等全部做完——如果你在第 1 步就能连打 10 层不觉得烦，说明房间循环本身站得住；如果打 3 层就腻，那问题在敌人波次生成（M-07），不在海克斯。

---

## 6. 一句话

**"能一直玩下去"的关键不是让系统无限供给内容，是让玩家主动选择要贪多深。** 出口机制（M-13）比层数上限重要得多——它把"我打了多久"从系统的失控变成玩家的决策，也顺手解决了无尽模式必然到来的数值通胀。
