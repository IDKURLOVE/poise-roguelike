# 破防之隙 · 100 小时扩展方案 v1.0

> 目标文件：`index.html`（单文件 HTML canvas，当前 ~3470 行 / ~128KB）
> 原则：模式入口按钮接入，不要求同屏；程序化复用现有管线，禁止灌水式刷时长。

---

## 1. 时长模型

### 1.1 预估小时数总表

| 板块 | 内容 | 预估小时 | 玩家行为画像 |
|------|------|---------|-------------|
| 无尽模式（现有） | 攀升 0→6、深度冲榜、海克斯图鉴 | 18h | 核心循环已成立，补充成就驱动 |
| 基础战役 | 12 层固定关卡 + 3 Boss | 12h | 首次通关 ~2h × 角色/攀升变体 |
| Boss Rush | 连续 5 Boss 车轮战 | 10h | 解锁后短局高难，重复挑战刷新纪录 |
| 竞技场波次 | 生存波次，无出口决策 | 12h | 每周波次排行榜 + 修饰器组合 |
| 挑战之书 | 20 条固定种子 + 限制规则 | 16h | 每条 ~45min × 重试，程序化生成 |
| 幻境 / 自定义 | 修饰器堆叠 + 花样组合 | 14h | 玩家自造规则，组合爆炸 |
| 教学 / 沙盒 | 机制教学 + 自由测试 | 3h | 新手必经 + 老手调参 |
| 内容池扩容体验 | 新海克斯/敌人/武器带来的构筑探索 | 15h | 新词条改变打法，图鉴驱动 |
| **合计** | | **≥100h** | |

### 1.2 为何不是灌水

| 维度 | 灌水做法 | 本方案做法 |
|------|---------|-----------|
| 模式差异 | 仅改怪物血量倍率 | 独立通关/失败条件、入口 UI、结算逻辑 |
| 内容复用 | 手工复制 50 关 | 程序化波次 + 种子生成挑战书 |
| 玩家动机 | "再打一遍" | 成就/图鉴/排行榜/自定义规则提供外驱 |
| 代码成本 | 每模式新写一套战斗 | 共用 `beginRoom` / `rebuildCfg` / `HEX_POOL` 管线 |

核心逻辑：**新模式 = 差异化入口逻辑 + 复用战斗核心**。玩家感知到的是新规则，而非重复劳动。

---

## 2. 模式大厅 UI 流程

```
┌─────────────┐
│   标题页     │  ← showTitle() 改造：从"开始"变为"进入大厅"
│  破防之隙    │
│  ◆ 货币/图鉴 │
└──────┬──────┘
       │ 点击「进入大厅」/ Enter
       ▼
┌─────────────────────────────────────┐
│           模式大厅 ModeHub           │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│  │无尽 │ │战役 │ │Boss │ │竞技场│   │
│  │ ★   │ │ 🔒  │ │ 🔒  │ │ 🔒  │   │
│  └─────┘ └─────┘ └─────┘ └─────┘   │
│  ┌─────┐ ┌─────┐ ┌─────┐            │
│  │挑战书│ │幻境 │ │沙盒 │            │
│  │ 🔒  │ │ 🔒  │ │ ★  │            │
│  └─────┘ └─────┘ └─────┘            │
│  ←→ 选择 · Enter 进入 · Esc 返回标题 │
└──────────────────┬──────────────────┘
                   │
       ┌───────────┼───────────┐
       ▼           ▼           ▼
   角色选择     攀升选择     直接开战
   (klass)    (ascension)  (部分模式跳过)
       │           │           │
       └───────────┴───────────┘
                   │
                   ▼
            startRun(modeConfig)
                   │
                   ▼
              战斗循环（现有）
```

### 2.1 状态机扩展

```js
// G.state 新增
"title" | "hub" | "modeSetup" | "playing" | "hexPick" | "dead" | "win" | "exit" | "pause"
```

### 2.2 入口数据结构

```js
const MODE_REGISTRY = {
  endless:   { id:"endless",   name:"无尽深渊", locked:false, onStart:startEndless },
  campaign:  { id:"campaign",  name:"基础战役", locked:true,  unlockCond:s=>s.best.depth>=10, onStart:startCampaign },
  bossRush:  { id:"bossRush",  name:"Boss连战", locked:true,  unlockCond:s=>s.best.depth>=20, onStart:startBossRush },
  arena:     { id:"arena",     name:"生存竞技场",locked:true, unlockCond:s=>s.best.depth>=8,  onStart:startArena },
  challenge: { id:"challenge", name:"挑战之书", locked:true,  unlockCond:s=>s.runs>=15,       onStart:startChallenge },
  dream:     { id:"dream",     name:"幻境构筑", locked:true,  unlockCond:s=>Object.keys(s.codex).length>=12, onStart:startDream },
  sandbox:   { id:"sandbox",   name:"沙盒试验", locked:false, onStart:startSandbox },
};
```

---

## 3. 模式详设（≥6）

### 模式 1：无尽深渊（现有无尽）

| 项 | 内容 |
|----|------|
| **入口名** | 无尽深渊 |
| **一句话定位** | 现有核心循环，深度冲榜 + 海克斯构筑 |
| **核心规则差异** | 每 5 层出口决策；海克斯随升级获取；攀升 0-6 叠加难度 |
| **通关条件** | 无（记录最深层数） |
| **失败条件** | HP 归零 |
| **解锁条件** | 默认解锁 |
| **预估小时** | 18h |
| **接口点** | `startRun()` → `openHexPick("start")` → `beginCombat()` → `beginRoom()` → `onRoomClear()` → `openExit()` / `confirmExit()` → `settle()` |

**改动**：`showTitle()` 的「开始」改为进入 `showHub()`；无尽作为默认高亮项。

---

### 模式 2：基础战役

| 项 | 内容 |
|----|------|
| **入口名** | 基础战役 |
| **一句话定位** | 12 层线性战役 + 3 个手工 Boss，有终局 |
| **核心规则差异** | 层池固定（非随机）；第 4/8/12 层为 Boss 房；无出口决策（每 4 层强制恢复 30% HP）；通关后进入下一难度（战役+） |
| **通关条件** | 击败第 12 层 Boss |
| **失败条件** | HP 归零（战役内死亡从本章起点重来，保留局外货币） |
| **解锁条件** | 最深层数 ≥ 10 |
| **预估小时** | 12h（首次通关 2h × 双角色 × 攀升变体） |
| **接口点** | 新增 `CAMPAIGN_ROOMS[12]` 数据表；`buildWave()` 检测 `G.mode==="campaign"` 时从表读取；`onRoomClear()` 检测 Boss 房走 `openBossRoom()`；`startRun({mode:"campaign"})` |

```js
// 战役层表（节选）
const CAMPAIGN_ROOMS = [
  { room:1,  pool:["grunt"],              count:3, waveMul:1.0 },
  { room:2,  pool:["grunt","caster"],     count:4, waveMul:1.1 },
  // ...
  { room:4,  pool:["boss_warden"],        count:1, waveMul:1.5, isBoss:true },
  // ...
  { room:12, pool:["boss_hollow_king"],   count:1, waveMul:3.0, isBoss:true },
];
```

---

### 模式 3：Boss 连战

| 项 | 内容 |
|----|------|
| **入口名** | Boss 连战 |
| **一句话定位** | 连续 5 场 Boss 车轮战，无小怪刷层 |
| **核心规则差异** | 每场 Boss 击败后恢复 20% HP + 获得 1 次海克斯选择；Boss 按顺序递进（精英 → 变体精英 → 战役 Boss）；计时排名 |
| **通关条件** | 击败全部 5 Boss |
| **失败条件** | HP 归零 |
| **解锁条件** | 最深层数 ≥ 20 |
| **预估小时** | 10h |
| **接口点** | 新增 `BOSS_RUSH_SEQUENCE[5]`；`beginRoom()` 检测 `G.mode==="bossRush"` 时 `spawnQueue=[bossType]`；每场击杀后 `openHexPick("bossRushContinue")`；复用 `spawnEnemy()` 新增 boss 类型 |

---

### 模式 4：生存竞技场

| 项 | 内容 |
|----|------|
| **入口名** | 生存竞技场 |
| **一句话定位** | 无尽波次生存，无出口决策，冲最高波数 |
| **核心规则差异** | 波次从 1 开始，每波敌人数量/质量递增；无出口节点（`exitEvery=∞`）；每 5 波奖励一次海克斯 + 恢复 15% HP；死亡后按波数结算 |
| **通关条件** | 无（记录最高波数） |
| **失败条件** | HP 归零 |
| **解锁条件** | 最深层数 ≥ 8 |
| **预估小时** | 12h |
| **接口点** | `buildWave()` 分支：`G.mode==="arena"` 时用 `arenaWaveMul(wave)` 替代 `waveMul(room)`；`onRoomClear()` 分支跳过 `openExit()` 直接 `beginRoom()`；`settle()` 按 `G.wave` 而非 `G.room` |

```js
// 竞技场波次曲线（与无尽 waveMul 不同，更陡）
function arenaWaveMul(w){
  return 1.0 * Math.pow(1.22, w - 1); // 每波 +22%，无分段硬拐
}
```

---

### 模式 5：挑战之书

| 项 | 内容 |
|----|------|
| **入口名** | 挑战之书 |
| **一句话定位** | 20 条固定种子 + 特殊限制，程序化可扩展 |
| **核心规则差异** | 每条挑战有：固定 RNG 种子、特殊限制（如"仅近战""无闪避""海克斯三选一→一选一""敌人 HP×2""禁止治疗球"）、目标层数；完成标记 ✓；支持每日轮换（种子 = 日期 hash） |
| **通关条件** | 达到挑战目标层数 |
| **失败条件** | HP 归零或违反限制（部分挑战） |
| **解锁条件** | 局数 ≥ 15 |
| **预估小时** | 16h（20 条 × ~45min × 重试） |
| **接口点** | 新增 `CHALLENGE_POOL[20]`；`startChallenge(idx)` 设置 `G.mode="challenge"` + `G.challenge=CHALLENGE_POOL[idx]`；`rebuildCfg()` 末尾应用 `G.challenge.restrictions`；`openHexPick()` 检测限制强制 `pickN=1`；RNG 种子：`G.rngSeed = G.challenge.seed`，在 `spawnEnemy` / `buildWave` 处替换 `Math.random()` 为 seeded |

```js
const CHALLENGE_POOL = [
  {
    id: "c01", name: "铁壁初试", seed: 1001,
    targetRoom: 10, klass: "melee",
    restrictions: { noRanged: true, enemyHpMul: 1.5 },
    reward: 40, desc: "近战限定 · 敌血×1.5 · 达到第 10 层"
  },
  {
    id: "c02", name: "空手道", seed: 1002,
    targetRoom: 8, klass: "any",
    restrictions: { noDodge: true, hexPickCount: 1 },
    reward: 50, desc: "禁止闪避 · 海克斯一选一 · 达到第 8 层"
  },
  // ... 共 20 条，程序化字段驱动
];
```

---

### 模式 6：幻境构筑（自定义规则）

| 项 | 内容 |
|----|------|
| **入口名** | 幻境构筑 |
| **一句话定位** | 玩家自由堆叠修饰器，自造规则组合 |
| **核心规则差异** | 进入前从修饰器池勾选 1-5 个；每个修饰器有正/负效果（如"弹丸+2 但伤害-30%""敌人移速+40% 但经验×2"）；堆叠数越多，◆ 结算倍率越高；可保存自定义组合 |
| **通关条件** | 无（记录最高层数 + 修饰器组合） |
| **失败条件** | HP 归零 |
| **解锁条件** | 图鉴（codex）≥ 12 个海克斯 |
| **预估小时** | 14h |
| **接口点** | 新增 `MOD_POOL[15]`；`startDream(modIds)` 设置 `G.mode="dream"` + `G.mods=modIds`；`rebuildCfg()` 末尾遍历 `G.mods` 调用各 `apply(C)`；`payout()` 乘以 `1 + 0.15 * G.mods.length` |

```js
const MOD_POOL = [
  { id:"m_rapid",  name:"迅捷之风", pos:{playerSpeedMul:1.3}, neg:{playerMaxHp:-20}, weight:2 },
  { id:"m_swarm",  name:"虫群",     pos:{expMul:1.8},          neg:{enemyCountMul:1.4}, weight:1 },
  { id:"m_glass",  name:"玻璃大炮", pos:{dmgMulGlobal:1.5},    neg:{playerMaxHp:-40}, weight:1 },
  { id:"m_poisond",name:"破防饥渴", pos:{breakTimeBonus:0.4},  neg:{poiseRegen:1.5}, weight:2 },
  // ... 共 15 个
];
```

---

### 模式 7：沙盒试验

| 项 | 内容 |
|----|------|
| **入口名** | 沙盒试验 |
| **一句话定位** | 自由调参测试场，无死亡压力 |
| **核心规则差异** | 可选：预设海克斯、敌人类型/数量、攀升层级、无敌开关；不计入图鉴/货币/纪录；一键重置 |
| **通关条件** | 无 |
| **失败条件** | 无（HP 归零自动回满） |
| **解锁条件** | 默认解锁 |
| **预估小时** | 3h（新手教学 + 老手调参） |
| **接口点** | `startSandbox()` 设置 `G.mode="sandbox"`；`beginRoom()` 检测 sandbox 跳过 `buildWave` 用 `G.sandboxQueue`；`damagePlayer()` 检测 sandbox `invuln` 时不扣血；`settle()` sandbox 分支不加货币 |

---

## 4. 挑战与成就（28 条）

### 4.1 按解锁条件分类

| ID | 名称 | 条件 | 类型 | 奖励 |
|----|------|------|------|------|
| A01 | 初次破防 | 完成 1 次破防击杀 | 教学 | ◆10 |
| A02 | 破防百次 | 累计破防 100 次 | 累计 | ◆30 |
| A03 | 破防千次 | 累计破防 1000 次 | 累计 | ◆100 |
| A04 | 无尽入门 | 无尽达到第 10 层 | 深度 | ◆20 |
| A05 | 无尽进阶 | 无尽达到第 25 层 | 深度 | ◆60 |
| A06 | 无尽深渊 | 无尽达到第 50 层 | 深度 | ◆150 |
| A07 | 竞技场新星 | 竞技场达到第 15 波 | 深度 | ◆30 |
| A08 | 竞技场王者 | 竞技场达到第 30 波 | 深度 | ◆80 |
| A09 | 战役通关 | 基础战役击败第 12 层 Boss | 通关 | ◆50 |
| A10 | 战役大师 | 战役+（二周目）通关 | 通关 | ◆120 |
| A11 | Boss 猎人 | Boss 连战全 5 场通关 | 通关 | ◆80 |
| A12 | Boss 无伤 | Boss 连战无伤通关 | 高难 | ◆200 |
| A13 | 近战专精 | 仅近战通关战役 | 限制 | ◆40 |
| A14 | 远程专精 | 仅远程通关战役 | 限制 | ◆40 |
| A15 | 无海克斯 | 不选任何海克斯达到第 15 层 | 限制 | ◆100 |
| A16 | 一选一 | 海克斯仅一选一模式达到第 12 层 | 限制 | ◆60 |
| A17 | 无治疗 | 全程不拾取治疗球达到第 15 层 | 限制 | ◆90 |
| A18 | 玻璃人 | 最大 HP ≤ 50 达到第 20 层 | 限制 | ◆110 |
| A19 | 攀升登顶 | 攀升 6 层级下通关任意模式 | 高难 | ◆200 |
| A20 | 挑战 5 条 | 完成挑战之书 5 条 | 累计 | ◆40 |
| A21 | 挑战全通 | 完成挑战之书全部 20 条 | 累计 | ◆200 |
| A22 | 幻境初探 | 幻境 3 修饰器组合达到第 15 层 | 累计 | ◆50 |
| A23 | 幻境狂人 | 幻境 5 修饰器组合达到第 12 层 | 高难 | ◆120 |
| A24 | 海克斯收藏家 | 图鉴收集 15 个海克斯 | 累计 | ◆50 |
| A25 | 海克斯全鉴 | 图鉴收集全部海克斯 | 累计 | ◆300 |
| A26 | 闪避大师 | 单局闪避成功 30 次 | 累计 | ◆40 |
| A27 | 破防艺术家 | 单局破防 25 次 | 累计 | ◆50 |
| A28 | 速通猎人 | 无尽 30 层内用时 < 8 分钟 | 高难 | ◆100 |

### 4.2 程序化生成规则

成就条目用数据驱动，检查函数统一：

```js
const ACHIEVEMENTS = [
  { id:"A04", name:"无尽入门", check: s => (s.best.depth||0) >= 10, reward:20 },
  { id:"A15", name:"无海克斯", check: run => run.hexes.length===0 && run.room>=15, reward:100 },
  // ...
];
// 局内检查：handleDeaths / onRoomClear 处调用
// 局外检查：settle() 后遍历 s.best / s.stats
```

---

## 5. 内容池扩容数量表

### 5.1 海克斯（HEX_POOL）

| 现有 | 新增 | 合计 | 新增方向 |
|------|------|------|---------|
| ~22 | +18 | 40 | 破防触发流、闪避反击流、弹丸轨道流、近战连击流、生命窃取流、时间停止流、孤勇流 |

新增示例（节选）：

| ID | 名称 | 品质 | 效果 | 代价 |
|----|------|------|------|------|
| hex_orbit | 环绕弹 | 彩 | 弹丸环绕自身旋转 3s | 弹速 -20% |
| hex_echo | 回响 | 金 | 破防后 1s 内下一次重击自动释放 | 重击前摇 +0.2s |
| hex_duel | 决斗者 | 银 | 对满血敌人首次伤害 ×2.5 | 对残血敌人伤害 -20% |
| hex_frenzy | 狂热 | 金 | 每秒无伤递增 3% 伤害（上限 30%） | 受伤后重置并 -10% 持续 3s |
| hex_blink | 闪现 | 彩 | 闪避变为短距离瞬移 | 闪避 CD +0.5s |

### 5.2 敌人类型

| 现有 | 新增 | 合计 | 说明 |
|------|------|------|------|
| grunt, caster, dasher, elite（4） | +4 | 8 | bomber, shielder, summoner, boss 变体 |

| 新敌人 | 定位 | 核心机制 |
|--------|------|---------|
| bomber | 自爆近战 | 冲刺→倒计时→范围爆炸，可打断 |
| shielder | 持盾推进 | 正面免疫削韧，需绕后或破防 |
| summoner | 远程召唤 | 定期召唤 1-2 个小怪，优先击杀 |
| boss_warden | 战役 Boss 1 | 三阶段：砸地→召唤→狂暴 |

### 5.3 武器/角色

| 现有 | 新增 | 合计 |
|------|------|------|
| melee「刃」, ranger「弦」 | +1 | 3 |

| 新角色 | 定位 | 机制差异 |
|--------|------|---------|
| lancer「枪」 | 中距离 | 直线突刺，破防效率高但范围窄；专属海克斯池 |

### 5.4 修饰器（MOD_POOL 用于幻境）

| 新增 | 说明 |
|------|------|
| 15 个 | 正/负效果配对，堆叠数决定奖励倍率 |

### 5.5 局外解锁（META_SHOP）

| 现有 | 新增 | 合计 |
|------|------|------|
| 3 | +5 | 8 |

| ID | 名称 | 成本 | 效果 |
|----|------|------|------|
| meta_char_lancer | 解锁「枪」 | 200 | 新角色可用 |
| meta_mode_campaign | 解锁战役 | 0 | 满足条件自动解锁 |
| meta_hex_slot | 海克斯槽位+1 | 180 | 本局可拥有海克斯上限+1 |
| meta_reroll | 抉择重掷 | 140 | 海克斯三选一时可重掷 1 次/局 |
| meta_start_armor | 开局护甲 | 110 | 开局获得 15 点虚血 |

---

## 6. 工程分期

### P0（地基 + 2 模式，~2 周）

| 任务 | 说明 | 涉及接口 |
|------|------|---------|
| 模式大厅 Hub UI | `showTitle()` → `showHub()`；`MODE_REGISTRY` | `G.state` 新增 `"hub"` |
| 模式配置注入 | `startRun({mode, ...})` 支持参数 | `startRun()`, `rebuildCfg()` |
| 无尽深渊迁移 | 现有逻辑标记 `G.mode="endless"` | `buildWave()`, `onRoomClear()` |
| 生存竞技场 | `arenaWaveMul()` + 无出口 | `buildWave()`, `onRoomClear()`, `settle()` |
| 基础战役（骨架） | 层表 + 1 个 Boss | `CAMPAIGN_ROOMS`, `spawnEnemy()` 新增 boss 类型 |
| 成就系统框架 | `ACHIEVEMENTS[]` + 检查钩子 | `settle()`, `handleDeaths()` |

**验收**：大厅可进入无尽/竞技场/战役；成就弹出正常。

### P1（内容填充，~2 周）

| 任务 | 说明 |
|------|------|
| Boss 连战模式 | 5 Boss 序列 + 计时 |
| 挑战之书（10 条） | 种子 + 限制规则 |
| 新敌人 bomber / shielder | AI + 绘制 + 经验值 |
| 新海克斯 ×10 | 覆盖破防流/闪避流/弹丸流 |
| 成就 ×15 | 深度/限制/累计类 |
| META_SHOP +4 | 解锁宽度 |

### P2（高难 + 自定义，~2 周）

| 任务 | 说明 |
|------|------|
| 幻境构筑 | MOD_POOL + 堆叠 UI |
| 挑战之书补至 20 条 | +每日轮换种子 |
| 新敌人 summoner + Boss 变体 | 战役 Boss 完整三阶段 |
| 新海克斯 ×8 | 彩色/高风险高回报 |
| 新角色 lancer「枪」 | 攻击管线分支 |
| 成就补至 28 | 高难/速通类 |

### P3（打磨 + 扩展，~2 周）

| 任务 | 说明 |
|------|------|
| 战役+ 二周目 | 数值重调 + 新词条 |
| 幻境组合保存/分享 | localStorage + 导出码 |
| 成就全量 28 条校验 | 自动化测试钩子 `__RG` |
| 数值平衡 pass | 各模式独立调参 |
| 性能优化 | 单文件体积控制，无用代码清理 |

---

## 7. 风险与对策

### 7.1 决策疲劳

| 风险 | 表现 | 对策 |
|------|------|------|
| 大厅选择过多 | 玩家不知道玩什么 | 默认高亮「上次模式」；未解锁项灰色但显示解锁条件 |
| 海克斯三选一频率过高 | 每 2 级弹窗打断节奏 | 攀升 5「窄门」已有二选一；幻境可禁用选择改为随机 |
| 修饰器堆叠选择 | 幻境 15 修饰器眼花 | 限制本局最多 5 个；提供「随机」按钮 |

### 7.2 数值通胀

| 风险 | 表现 | 对策 |
|------|------|------|
| 海克斯叠加失控 | 伤害 ×100 瞬秒 | `rebuildCfg()` 已有 `enemyHpMul` 上限 12；新增 `dmgMulGlobal` 上限 5 |
| 多模式货币刷取 | 竞技场速刷 ◆ | 各模式 `payout()` 独立倍率；挑战/幻境不给额外 ◆ |
| 攀升 + 模式难度叠加 | 攀升 6 + 幻境 5 修饰器 = 不可玩 | 检测组合，提示「此组合无额外奖励」 |

### 7.3 单文件体积

| 风险 | 当前 | 扩展后预估 | 对策 |
|------|------|-----------|------|
| 文件大小 | ~128KB | ~280-350KB | CSS/JS 压缩；注释精简；成就/挑战数据用紧凑 JSON |
| 加载时间 | <50ms | <150ms | 可接受（纯本地文件） |
| 维护难度 | 中 | 高 | 严格模块注释分隔；`// ========== 模式: xxx ==========` |
| Git diff 噪音 | 中 | 高 | 单文件无法避免；考虑 P3 后拆分 `game.js` + `modes.js`（非阻塞） |

### 7.4 接口污染

| 风险 | 对策 |
|------|------|
| `beginRoom` / `buildWave` / `onRoomClear` 里塞满 if(mode) | 用 `MODE_HOOKS[mode] = { onRoomEnter, onRoomClear, buildWave, settle }` 策略模式 |
| `rebuildCfg` 无限膨胀 | 修饰器/攀升/海克斯统一走 `apply(C)` 管线，禁止直接改 CFG |

---

## 附录：关键现有接口速查

| 函数/状态 | 位置（约行号） | 职责 |
|-----------|---------------|------|
| `startRun()` | 3342 | 重置局内状态，打开首次海克斯选择 |
| `beginRoom(isFirst)` | 3009 | 重置房间，调 `buildWave`，首怪立即出场 |
| `onRoomClear()` | 3108 | 发经验，层+1，判断出口 |
| `buildWave(room)` | 3082 | 按层生成 `spawnQueue` |
| `spawnEnemy(type,x,y,mul)` | 1439 | 按类型实例化敌人 |
| `rebuildCfg()` | 775 | 纯函数合并海克斯+攀升+角色到 `G.C` |
| `openHexPick(resume)` | 2838 | 打开海克斯三选一 |
| `settle(reason)` | 1367 | 结算货币、更新图鉴、写存档 |
| `showTitle()` | 2658 | 标题页渲染（将改为进入 Hub） |
| `HEX_POOL` | 502 | 海克斯定义数组 |
| `ASCENSION` | 765 | 攀升 0-6 定义 |
| `META_SHOP` | 1314 | 局外解锁 3 项 |
| `G.mode` | **新增** | 当前模式标识 |
| `G.state` | 1071 | `title\|hub\|playing\|...` |

---

*文档版本：v1.0 · 对应代码基线：index.html @ 2026-09-15*
